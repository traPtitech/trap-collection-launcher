import React, { useEffect } from 'react';
import styled from 'styled-components';

export type Props = { someArg?: string };

const Modal = ({ someArg }: Props) => {
  return <></>;
};

export default Modal;
